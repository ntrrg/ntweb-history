os: linux
dist: xenial
git:
  depth: 1
language: go
go: "1.13"
env:
  - GO111MODULE=on
jobs:
  include:
    - stage: "Linting"
      name: "Go"
      script: ./mage -v lint:go
    - name: "Markdown"
      script: ./mage -v lint:md
    - stage: "Build"
      script: ./mage -v build:docker

