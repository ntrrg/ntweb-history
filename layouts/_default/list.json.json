{{ define "data" }}
  {
    "pages": [
      {{ range $i, $val := .Paginator.Pages }}
        {{ if ne $i 0 }},{{ end }}
        {
          "url": {{ jsonify .Permalink }},
          "title": {{ jsonify .Title }},
          "description": {{ jsonify .Description }}
        }
      {{ end }}
    ],
    "paginationFormat": "{{ .Permalink }}page/%d/index.json",
    "prev": "{{ with .Paginator.Prev }}{{ .URL }}{{ end }}",
    "next": "{{ with .Paginator.Next }}{{ .URL }}{{ end }}"
  }
{{ end }}

